A project for EPFL Interactive Theorem Proving course (CS-428). \
In this project, we formalize a subset of GDScript, covering variable assignment, method declaration, the process and ready functions, statements such as branching, but more importantly the handling of signals through awaiting and emitting. We validated our semantics through several example programs, all behaving as expected.

